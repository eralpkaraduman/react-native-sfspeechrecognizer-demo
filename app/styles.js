import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  screen: {
    paddingTop: 20,
    backgroundColor: 'white',
    alignSelf: 'stretch',
    width: '100%',
    height: '100%',
  }
});
